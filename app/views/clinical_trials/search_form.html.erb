<h1>Clinical Trials Search</h1>

<%= form_with url: perform_search_clinical_trials_path, method: :post, local: true do |form| %>
  <div class="form-group">
    <%= form.label :query, "Enter your search query:" %>
    <%= form.text_field :query, class: "form-control", placeholder: "e.g., cardiac repolarization" %>
  </div>

  <div class="form-group mt-3">
    <%= form.label :threshold, "Similarity Threshold:" %>
    <div class="input-group">
      <%= form.number_field :threshold, value: 0.5, min: 0.0, max: 1.0, step: 0.05, class: "form-control" %>
      <span class="input-group-text">0.0 to 1.0</span>
    </div>
    <small class="form-text text-muted">
      0.5 is optimal for perfect recall, but you can increase it to improve precision over recall.
    </small>
  </div>

  <div class="form-group mt-3">
    <%= form.submit "Search", class: "btn btn-primary" %>
  </div>
<% end %>

<!-- Display flash messages -->
<% if flash[:alert] %>
  <div class="alert alert-danger mt-3"><%= flash[:alert] %></div>
<% elsif flash[:notice] %>
  <div class="alert alert-info mt-3"><%= flash[:notice] %></div>
<% end %>
